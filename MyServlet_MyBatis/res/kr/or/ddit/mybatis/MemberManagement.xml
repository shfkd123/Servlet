<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MemManage">
	
	<!-- 전체회원 리스트 출력 -->
	<select id="memberAllListPrint" resultType="member" parameterType="member">
		SELECT MEM_ID     memId
	 	       ,MEM_PASS   memPass
	 	       ,MEM_NAME   memName
	 	       ,MEM_HP     memHp
	 	       ,MEM_EMAIL  memEmail
		FROM MEMBER_MANAGEMENT
	</select>
	
	<!-- 회원정보등록 -->
	<insert id="memberInfoInsert" parameterType="member">
		INSERT INTO MEMBER_MANAGEMENT(MEM_ID, MEM_NAME, MEM_PASS, MEM_HP, MEM_EMAIL)
		VALUES(#{memId},#{memName},#{memPass},#{memHp},#{memEmail})
	</insert>
	
	<!-- 회원정보수정 -->
	<update id="memberInfoUpdate" parameterType="member">
		UPDATE MEMBER_MANAGEMENT
		SET MEM_PASS = #{memPass}	
			,MEM_HP = #{memHp}
			,MEM_EMAIL = #{memEmail}
		WHERE MEM_ID = #{memId}
	</update>
	
	<!-- 회원정보삭제 -->
	<delete id="memberInfoDelete" parameterType="member">
		DELETE FROM MEMBER_MANAGEMENT
		WHERE MEM_ID = #{memId}
	</delete>
	
	<!-- 회원정보상세조회 -->
	<select id="getMemberInfo" resultType="member" parameterType="member">
		SELECT MEM_ID     memId
	 	       ,MEM_PASS   memPass
	 		   ,MEM_NAME   memName
	 	       ,MEM_HP     memHp
	 	       ,MEM_EMAIL  memEmail
		FROM MEMBER_MANAGEMENT
		WHERE MEM_ID = #{memId}
	</select>
	
	<!-- 회원아이디로 회원 존재여부 체크 -->
	<select id="checkMember" resultType="member" parameterType="member">
		SELECT COUNT(*) AS CNT 
		FROM MEMBER_MANAGEMENT WHERE MEM_ID = #{memId}
	</select>
</mapper>